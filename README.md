
#### 1) What is the difference between `null` and `undefined`?
     null মানে হচ্ছে যে - কোনো variable এর মান নেই এমন সেট করা হয়েছে ।
    undefined মানে হচ্ছে যে - কোনো variable দেওয়া হয়েছে এর মান সেট দেওয়া হয় নি/

#### 2) What is the use of the `map()` function in JavaScript? How is it different from `forEach()`?
    map() function ১টি array এর সব মানের উপর কাজ করে ও নতুন array return করে।
     forEach() সব মানের উপর কাজ করে কিন্তু নতুন array return করে না।

#### 3) What is the difference between `==` and `===`?
    == শুদু মান (value ) চেক করে।
    === মান (value) ও টাইপ চেক করে।

#### 4) What is the significance of `async`/`await` in fetching API data?
    async function asynchronous করে 
    await - তারা কাজ শেষ না হওয়া পর্যন্ত অপেক্ষা করে
#### 5) Explain the concept of Scope in JavaScript (Global, Function, Block).
        Global - variable যা function এর বাইরে declear করা হয় ।
        Function -variable যা function এর ভিতরে declear করা হয় ।
        Block - variable {} যা  ভিতরে declear করা হয় . if /for . 